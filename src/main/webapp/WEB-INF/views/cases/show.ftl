{
"id" : ${case.id},
"name" : "${(case.name)!}",
"num": "${(case.num)!}!",
"status": "${(case.status)!}",
"memo": "${(case.memo)!}",
"started_at": "${(case.started_at)!}",
"ended_at": "${(case.ended_at)!}"
}