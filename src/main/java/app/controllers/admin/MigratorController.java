package app.controllers.admin;

import app.controllers.APIController;

public class MigratorController extends APIController {
  public void migrate() {
    // new MigrationManager(getMigrationsPath()).migrate(getLog(), getEncoding());
  }
}
